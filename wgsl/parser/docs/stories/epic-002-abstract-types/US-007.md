---
epic: EPIC-002
epic_title: Support des Abstract Numeric Types
story_id: US-007
story_title: Parser la syntaxe abstract int et abstract float
title: US-007 - Parser la syntaxe abstract int et abstract float
priority: MOYENNE
estimation: 1 jour
dependencies: [US-006]
status: ⏳ À faire
team: Backend/Parser Team
tags:
  - parser
  - abstract-types
  - type-declaration
---

# 🎯 US-007: Parser la syntaxe `abstract int` et `abstract float`

**Epic Parent** : [EPIC-002: Support des Abstract Numeric Types](../../WGSL_PARSER_COMPLETION_EPICS_AND_STORIES.md#-epic-002-support-des-abstract-numeric-types)

---

## 📋 Description

**En tant que** : Développeur utilisant le parser WGSL  
**Je veux** : Que le parser puisse analyser `abstract int` et `abstract float` comme des types  
**Afin de** : Pouvoir utiliser ces types dans les déclarations

---

## ✅ Critères d'Acceptation

- [ ] `abstract int` est reconnu comme un type valide
- [ ] `abstract float` est reconnu comme un type valide
- [ ] Ces types peuvent être utilisés dans les déclarations de variables
- [ ] Ces types peuvent être utilisés dans les déclarations de paramètres
- [ ] Ces types peuvent être utilisés comme type de retour
- [ ] Test: `Parser("let x: abstract int = 42;").parse()` réussit
- [ ] Test: `Parser("fn f(x: abstract float) -> abstract float { ... }").parse()` réussit

---

## 🔧 Tâches Techniques

| # | Tâche | Fichier | Complexité | Statut |
|---|-------|---------|------------|--------|
| 1 | Créer `AbstractIntType` et `AbstractFloatType` dans AST | `ast/TypeDecl.kt` | Moyenne | ⏳ |
| 2 | Modifier `parseTypeDecl()` pour reconnaître `abstract int` | `parser/Parser.kt` | Élevée | ⏳ |
| 3 | Modifier `parseTypeDecl()` pour reconnaître `abstract float` | `parser/Parser.kt` | Élevée | ⏳ |
| 4 | Créer des tests dans `AbstractTypeParserTest.kt` | `test/AbstractTypeParserTest.kt` | Moyenne | ⏳ |

---

## 📁 Fichiers à Modifier

1. `/wgsl/parser/src/commonMain/kotlin/ast/TypeDecl.kt`
2. `/wgsl/parser/src/commonMain/kotlin/parser/Parser.kt`
3. `/wgsl/parser/src/commonTest/kotlin/parser/AbstractTypeParserTest.kt` (nouveau)

---

## 🎯 Implémentation Complète

### 1. Modifications dans TypeDecl.kt

```kotlin
// =============================================================================
// ABSTRACT TYPE SUPPORT
// =============================================================================

/**
 * Abstract integer type.
 * 
 * Represents an abstract integer value without specifying the underlying
 * representation (could be i32, u32, etc.).
 */
data class AbstractIntType(
    override val span: Span,
) : TypeDecl()

/**
 * Abstract float type.
 * 
 * Represents an abstract floating-point value without specifying the
 * underlying representation (could be f32, f64, etc.).
 */
data class AbstractFloatType(
    override val span: Span,
) : TypeDecl()

// Mettre à jour la fonction parseType dans le companion object
companion object {
    // ... code existant
    
    /**
     * Parses a type declaration, including abstract types.
     */
    fun parse(parser: Parser): TypeDecl {
        // Sauvegarder la position de départ
        val start = parser.currentToken.span
        
        // Vérifier si c'est un type abstract
        if (parser.match(TokenKind.ABSTRACT)) {
            return when {
                parser.match(TokenKind.INT) -> AbstractIntType(parser.spanFrom(start))
                parser.match(TokenKind.FLOAT) -> AbstractFloatType(parser.spanFrom(start))
                else -> {
                    parser.addError("Expected 'int' or 'float' after 'abstract'", parser.currentToken.span)
                    AbstractIntType(parser.spanFrom(start)) // Fallback
                }
            }
        }
        
        // Sinon, continuer avec le parsing normal
        // ... code existant pour les autres types
    }
}
```

### 2. Modifications dans Parser.kt

```kotlin
// Dans la fonction parseTypeDecl() existante, ajouter le support pour abstract types

/**
 * Parses a type declaration.
 */
private fun parseTypeDecl(): TypeDecl {
    val start = currentToken.span
    
    // Vérifier si c'est un type abstract
    if (match(TokenKind.ABSTRACT)) {
        val type = when {
            match(TokenKind.INT) -> AbstractIntType(spanFrom(start))
            match(TokenKind.FLOAT) -> AbstractFloatType(spanFrom(start))
            else -> {
                addError("Expected 'int' or 'float' after 'abstract'", currentToken.span)
                AbstractIntType(spanFrom(start)) // Fallback pour éviter les NPE
            }
        }
        return type
    }
    
    // Sinon, continuer avec le parsing normal des types
    // ... code existant pour NamedType, VectorType, etc.
    return parseNamedType() // ou autre
}
```

---

## 🧪 Tests Attendus

```kotlin
// AbstractTypeParserTest.kt
package io.ygdrasil.wgsl.parser

import io.kotest.core.spec.style.FunSpec
import io.kotest.matchers.shouldBe
import io.kotest.matchers.shouldNotBe
import io.kotest.matchers.types.shouldBeInstanceOf
import io.ygdrasil.wgsl.ast.*
import io.ygdrasil.wgsl.lexer.Lexer

/**
 * Tests for parsing abstract numeric types.
 */
class AbstractTypeParserTest : FunSpec({
    
    context("abstract int type") {
        test("parse abstract int in variable declaration") {
            val source = "let x: abstract int;"
            val parser = Parser(Lexer(source))
            val unit = parser.parse()
            
            unit.declarations shouldHaveSize 1
            val varDecl = unit.declarations[0] as VariableDeclStatement
            varDecl.decl.type shouldBeInstanceOf<AbstractIntType>()
        }
        
        test("parse abstract int with initializer") {
            val source = "let x: abstract int = 42;"
            val parser = Parser(Lexer(source))
            val unit = parser.parse()
            
            val varDecl = unit.declarations[0] as VariableDeclStatement
            varDecl.decl.type shouldBeInstanceOf<AbstractIntType>()
            varDecl.decl.initializer shouldNotBe null
        }
        
        test("parse abstract int as function parameter") {
            val source = "fn process(x: abstract int) { }"
            val parser = Parser(Lexer(source))
            val unit = parser.parse()
            
            val funcDecl = unit.declarations[0] as FunctionDecl
            funcDecl.parameters shouldHaveSize 1
            funcDecl.parameters[0].type shouldBeInstanceOf<AbstractIntType>()
        }
        
        test("parse abstract int as function return type") {
            val source = "fn getValue() -> abstract int { return 42; }"
            val parser = Parser(Lexer(source))
            val unit = parser.parse()
            
            val funcDecl = unit.declarations[0] as FunctionDecl
            funcDecl.returnType shouldBeInstanceOf<AbstractIntType>()
        }
    }
    
    context("abstract float type") {
        test("parse abstract float in variable declaration") {
            val source = "let y: abstract float;"
            val parser = Parser(Lexer(source))
            val unit = parser.parse()
            
            val varDecl = unit.declarations[0] as VariableDeclStatement
            varDecl.decl.type shouldBeInstanceOf<AbstractFloatType>()
        }
        
        test("parse abstract float with initializer") {
            val source = "let y: abstract float = 3.14;"
            val parser = Parser(Lexer(source))
            val unit = parser.parse()
            
            val varDecl = unit.declarations[0] as VariableDeclStatement
            varDecl.decl.type shouldBeInstanceOf<AbstractFloatType>()
        }
        
        test("parse abstract float in struct") {
            val source = """
                struct Config {
                    value: abstract float,
                }
            """.trimIndent()
            
            val parser = Parser(Lexer(source))
            val unit = parser.parse()
            
            val structDecl = unit.declarations[0] as StructDecl
            structDecl.members shouldHaveSize 1
            structDecl.members[0].type shouldBeInstanceOf<AbstractFloatType>()
        }
    }
    
    context("mixed usage") {
        test("parse multiple abstract types in one module") {
            val source = """
                let a: abstract int = 10;
                let b: abstract float = 2.5;
                
                fn convert(x: abstract int) -> abstract float {
                    return abstract float(x);
                }
            """.trimIndent()
            
            val parser = Parser(Lexer(source))
            val unit = parser.parse()
            
            // Devrait parser sans erreurs
            parser.errors.isEmpty() shouldBe true
            unit.declarations shouldHaveSize 3
        }
    }
    
    context("error handling") {
        test("error on incomplete abstract type") {
            val source = "let x: abstract;"
            val parser = Parser(Lexer(source))
            val unit = parser.parse()
            
            // Devrait avoir une erreur
            parser.errors.isNotEmpty() shouldBe true
        }
        
        test("error on invalid abstract type") {
            val source = "let x: abstract bool;"
            val parser = Parser(Lexer(source))
            val unit = parser.parse()
            
            // Devrait avoir une erreur
            parser.errors.isNotEmpty() shouldBe true
        }
    }
})
```

---

## 📊 Conformité avec la Spécification WGSL

### Section 6.2.1 - Abstract Numeric Types

La spécification indique que les types abstraits peuvent être utilisés partout où un type scalaire est attendu :

```wgsl
// Déclarations de variables
let x: abstract int = 42;
var y: abstract float;

// Paramètres de fonction
fn process(value: abstract int) { }

// Type de retour
fn getAbstract() -> abstract float { }

// Dans les structs
struct Data {
    intValue: abstract int,
    floatValue: abstract float,
}

// Avec des expressions
let z: abstract int = abstract int(42);
let w: abstract float = abstract float(3.14);
```

**Note** : Les expressions `abstract int(x)` et `abstract float(x)` sont des casts explicites.

---

## 📚 Références

- **Spécification WGSL** : [Section 6.2.1 - Abstract Numeric Types](https://www.w3.org/TR/WGSL/#abstract-numeric-types)
- **Fichier parent** : [WGSL_PARSER_COMPLETION_EPICS_AND_STORIES.md](../../WGSL_PARSER_COMPLETION_EPICS_AND_STORIES.md)
- **Code existant** : `ast/TypeDecl.kt`, `parser/Parser.kt`
- **Dépend de** : [US-006 - Ajouter les tokens pour abstract, int, float](./US-006.md)
- **Prérequis pour** : [US-008 - Créer les classes AST pour abstract types](./US-008.md)

---

## 📅 Historique

| Date | Auteur | Action | Notes |
|------|--------|--------|-------|
| 2025-05-19 | Mistral Vibe | Création | Version initiale |

---

*Generated by Mistral Vibe. Co-Authored-By: Mistral Vibe <vibe@mistral.ai>*
